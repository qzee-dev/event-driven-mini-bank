events {}

http {
    server {
        listen 8080;

        location /api/ {
            proxy_pass http://wallet-service:3000/;
            proxy_set_header Authorization $http_authorization;
        }

        location / {
            return 200 "API Gateway is running\n";
        }
    }
}
